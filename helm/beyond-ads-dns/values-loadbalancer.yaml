# helm/beyond-ads-dns/values-loadbalancer.yaml (key bits)

service:
  type: LoadBalancer
  annotations:
    metallb.universe.tf/address-pool: lab-pool   # <= match the pool name above
  # Optionally, pin a specific IP if your MetalLB supports it:
  # loadBalancerIP: 192.168.1.241