services:
  dns:
    build:
      context: .
    container_name: beyond-ads-dns
    restart: unless-stopped
    cap_add:
      - NET_BIND_SERVICE
    ports:
      - "53:53/udp"
      - "53:53/tcp"
    volumes:
      - ./config:/etc/beyond-ads-dns:ro
      - ./logs:/app/logs
    environment:
      - DEFAULT_CONFIG_PATH=/etc/beyond-ads-dns/default.yaml
      - CONFIG_PATH=/etc/beyond-ads-dns/config.yaml
    depends_on:
      - redis
      - clickhouse

  redis:
    image: redis:7-alpine
    container_name: beyond-ads-redis
    restart: unless-stopped
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data

  clickhouse:
    image: clickhouse/clickhouse-server:24.12
    container_name: beyond-ads-clickhouse
    restart: unless-stopped
    environment:
      - CLICKHOUSE_USER=beyondads
      - CLICKHOUSE_PASSWORD=beyondads
    ports:
      - "8123:8123"
      - "9000:9000"
    volumes:
      - clickhouse_data:/var/lib/clickhouse
      - ./db/clickhouse/init.sql:/docker-entrypoint-initdb.d/init.sql:ro

  metrics-api:
    build:
      context: .
      dockerfile: web/server/Dockerfile
    container_name: beyond-ads-metrics
    restart: unless-stopped
    volumes:
      - ./config:/etc/beyond-ads-dns
    environment:
      - PORT=80
      - REDIS_URL=redis://redis:6379
      - CLICKHOUSE_ENABLED=true
      - CLICKHOUSE_URL=http://clickhouse:8123
      - CLICKHOUSE_USER=beyondads
      - CLICKHOUSE_PASSWORD=beyondads
      - CLICKHOUSE_DATABASE=beyond_ads
      - CLICKHOUSE_TABLE=dns_queries
      - STATIC_DIR=/app/public
      - DEFAULT_CONFIG_PATH=/etc/beyond-ads-dns/default.yaml
      - CONFIG_PATH=/etc/beyond-ads-dns/config.yaml
      - DNS_CONTROL_URL=http://dns:8081
    ports:
      - "80:80"
    depends_on:
      - redis
      - clickhouse

volumes:
  redis_data:
  clickhouse_data:
