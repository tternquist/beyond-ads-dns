# Required for Let's Encrypt
LETSENCRYPT_DOMAIN=your-domain.com
LETSENCRYPT_EMAIL=admin@your-domain.com

# Optional: use staging server (for testing, avoids rate limits)
# LETSENCRYPT_STAGING=true

# Optional: use DNS-01 challenge when port 80 is not reachable
# LETSENCRYPT_CHALLENGE_TYPE=dns
# LETSENCRYPT_DNS_PROPAGATION_WAIT=120

# Optional: hostname shown in the Metrics UI
# HOSTNAME=my-dns-server

# DoH/DoT server: uses Let's Encrypt certs (enabled by default)
# Set to false to disable encrypted DNS (DoT on 853, DoH on 8443)
# DOH_DOT_ENABLED=true
