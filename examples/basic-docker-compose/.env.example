# Optional: hostname shown in the Metrics UI and usage stats webhook
# HOSTNAME=my-dns-server

# Optional: host IP for usage stats webhook. The compose file includes extra_hosts so
# host.docker.internal resolves on Linux. Set HOST_IP to override with your LAN IP (e.g. 192.168.1.10).
# HOST_IP=192.168.1.10
