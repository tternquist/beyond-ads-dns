# Optional: hostname shown in the Metrics UI and usage stats webhook
# HOSTNAME=my-dns-server

# Optional: host IP for usage stats webhook (when running in Docker, set to your host's IP
# if host.docker.internal is not available, e.g. Linux without extra_hosts)
# HOST_IP=192.168.1.10
