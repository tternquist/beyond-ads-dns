# Optional: hostname shown in the Metrics UI
# HOSTNAME=my-dns-server

# Optional: enable login - set password via env or use `beyond-ads-dns set-admin-password` in container
# UI_PASSWORD=your-secure-password

# Optional: session secret for production (auto-generated if not set)
# SESSION_SECRET=random-32-byte-hex-string

# Optional: HTTPS
# HTTPS_ENABLED=true
# SSL_CERT_FILE=/path/to/cert.pem
# SSL_KEY_FILE=/path/to/key.pem
# HTTPS_PORT=443
