# Optional: hostname shown in the Metrics UI
# HOSTNAME=my-dns-server

# Optional: enable login - set password via env or use `beyond-ads-dns set-admin-password` in container
# UI_PASSWORD=your-secure-password

# Optional: session secret for production (auto-generated if not set)
# SESSION_SECRET=random-32-byte-hex-string

# Optional: HTTPS (manual certificates)
# HTTPS_ENABLED=true
# SSL_CERT_FILE=/path/to/cert.pem
# SSL_KEY_FILE=/path/to/key.pem
# HTTPS_PORT=443

# Optional: Let's Encrypt (automatic HTTPS)
# LETSENCRYPT_ENABLED=true
# LETSENCRYPT_DOMAIN=example.com
# LETSENCRYPT_EMAIL=admin@example.com
# LETSENCRYPT_CERT_DIR=/app/letsencrypt
# LETSENCRYPT_STAGING=false
# LETSENCRYPT_CHALLENGE_TYPE=http
#   Use "dns" when port 80 is not reachable - add TXT records manually
# LETSENCRYPT_DNS_PROPAGATION_WAIT=120
#   Seconds to wait for DNS propagation before completing DNS-01 challenge
